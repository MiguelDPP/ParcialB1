import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devjsph.camaraaguachica',
  appName: 'Camara Aguachica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
